# Copy to .env.local and fill in from Supabase -> Project Settings -> API.
# .env.local is gitignored. The anon key is safe in the browser; row level
# security is what protects the data.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
